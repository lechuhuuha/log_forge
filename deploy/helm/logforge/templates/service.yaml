apiVersion: v1
kind: Service
metadata:
  name: {{ include "logforge.fullname" . }}
  labels:
    {{- include "logforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "logforge.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
