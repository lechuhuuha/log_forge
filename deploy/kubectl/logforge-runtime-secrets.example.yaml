apiVersion: v1
kind: Secret
metadata:
  name: logforge-dev-runtime
  namespace: dev
type: Opaque
stringData:
  dev_auth_keys_json: '["CHANGE_ME_DEV_API_KEY"]'
  # Lab default: must match secret/minio-root-creds in namespace storage.
  dev_minio_access_key: "minioadmin"
  dev_minio_secret_key: "5mpeVdS&KDn3Ks)"
---
apiVersion: v1
kind: Secret
metadata:
  name: logforge-staging-runtime
  namespace: staging
type: Opaque
stringData:
  auth_keys_json: '["CHANGE_ME_STAGING_API_KEY"]'
  # Lab default: must match secret/minio-root-creds in namespace storage.
  minio_access_key: "minioadmin"
  minio_secret_key: "5mpeVdS&KDn3Ks)"
---
apiVersion: v1
kind: Secret
metadata:
  name: logforge-production-runtime
  namespace: production
type: Opaque
stringData:
  production_auth_keys_json: '["CHANGE_ME_PRODUCTION_API_KEY"]'
  # Lab default: must match secret/minio-root-creds in namespace storage.
  production_minio_access_key: "minioadmin"
  production_minio_secret_key: "5mpeVdS&KDn3Ks)"
